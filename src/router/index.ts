import * as React from 'react';
import {
    BrowserRouter as Router,
    Route,
    Link,
    Redirect,
    Switch
} from 'react-router-dom'
import * as App from '../page/app/app';

const router = (
    <Router>
    <App>
    </App>
    < /Router>
)

export default router; 